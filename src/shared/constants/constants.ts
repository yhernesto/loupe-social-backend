export const SCORE_POSTS_LIMIT = 10

export enum TextType {
  POST_CAPTION = 0,
  COMMENT = 1
}

export enum SocialNetwork {
  FACEBOOK = 0,
  INSTAGRAM = 1,
  TWITTER = 2,
  TIKTOK = 3
}

export enum ExecutionState {
  OK = 0,
  ERROR_RESP_API = 1
}